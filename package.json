{
  "name": "ng-shadcn",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "cli"
  ],
  "scripts": {
    "ng": "ng",
    "start": "ng serve demo",
    "build": "ng build demo",
    "build:lib": "npm run build:components",
    "build:components": "ng build button && ng build input && ng build dialog && ng build tooltip && ng build card && ng build switch && ng build select && ng build checkbox && ng build radio-group && ng build textarea && ng build badge && ng build alert && ng build tabs && ng build dropdown-menu && ng build popover && ng build accordion",
    "build:components:production": "ng build button --configuration production && ng build input --configuration production && ng build dialog --configuration production && ng build tooltip --configuration production && ng build card --configuration production && ng build switch --configuration production && ng build select --configuration production && ng build checkbox --configuration production && ng build radio-group --configuration production && ng build textarea --configuration production && ng build badge --configuration production && ng build alert --configuration production && ng build tabs --configuration production && ng build dropdown-menu --configuration production && ng build popover --configuration production && ng build accordion --configuration production",
    "build:cli": "cd cli && npm run build",
    "build:production": "npm run build:components:production && npm run build:cli",
    "build:analyze": "node scripts/bundle-analyzer.js",
    "build:optimize": "node scripts/build-optimizer.js",
    "dev": "ng serve demo --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "test:ssr": "node scripts/ssr-tester.js",
    "cli": "node cli/dist/index.js",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lint": "ng lint",
    "phase4": "npm run build:optimize && npm run build:analyze && npm run test:ssr"
  },
  "dependencies": {
    "@angular/animations": "^19.0.0",
    "@angular/common": "^19.0.0",
    "@angular/compiler": "^19.0.0",
    "@angular/core": "^19.0.0",
    "@angular/forms": "^19.0.0",
    "@angular/platform-browser": "^19.0.0",
    "@angular/router": "^19.0.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "rxjs": "^7.8.1",
    "tailwindcss": "^4.1.11",
    "tslib": "^2.5.0",
    "zone.js": "~0.15.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^19.0.0",
    "@angular/build": "^19.0.0",
    "@angular/cli": "^19.0.0",
    "@angular/compiler-cli": "^19.0.0",
    "@types/fs-extra": "^11.0.0",
    "@types/inquirer": "^9.0.0",
    "@types/node": "^20.0.0",
    "commander": "^11.0.0",
    "fs-extra": "^11.0.0",
    "inquirer": "^9.0.0",
    "ng-packagr": "^19.0.0",
    "typescript": "~5.8.2",
    "jest": "^29.7.0",
    "jest-preset-angular": "^14.2.0",
    "@types/jest": "^29.5.0",
    "@storybook/angular": "^8.3.0",
    "@storybook/addon-essentials": "^8.3.0",
    "@storybook/addon-interactions": "^8.3.0",
    "@storybook/addon-links": "^8.3.0",
    "@storybook/blocks": "^8.3.0",
    "@storybook/test": "^8.3.0",
    "@playwright/test": "^1.40.0",
    "storybook": "^8.3.0"
  }
}